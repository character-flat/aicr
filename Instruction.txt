python -m venv venv  
source venv/bin/activate  # On Windows: venv\\Scripts\\activate  

pip install fastapi uvicorn  

uvicorn main:app --host 0.0.0.0 --port 8000 --reload
expose port 8000, by setting visiblity in ports to public
Then visit GH repo/settings/webhook, change url to the dynamically gen. codespace url (append /webhook)